---
- name: test
  hosts: localhost
  gather_facts: no
  tasks:

  - name: Add EE to the controller instance
    awx.awx.execution_environment:
      name: "My EE"
      image: ablecloudteam/genie-awx-ee
